name: IntelliStore CI

on:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check Repository Directories
        run: |
          echo "Verifying enterprise directory structure..."
          ls -la backend frontend ai-service

